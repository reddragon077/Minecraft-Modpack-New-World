NewWorldCore 0.5.60.2 radar, Field Survey isolation, route cleanup, range, dynamic-filter, config-suite, and Geology-flush patch

- Structure Radar reads the live structure registry, including modded namespaces.
- Modded structures are queried individually with at most one dynamic locate per server tick.
- Registry variants are normalized into shared discovery families.
- Shared multi-family placement candidates remain UNKNOWN until Field Survey confirmation.
- Geology worldgen structures are excluded from Structure Radar and Field Survey.
- Persisted false MODDED STRUCTURE and geology-as-structure records are removed safely.
- Removed legacy selected targets are reset without breaking arrival checks.
- Structure filter overlays render above radar results and telemetry text.
- Selected dynamic structure families remove every non-selected placement result.
- Placement candidates honor the displayed upgrade range; selected families prune unrelated placement tasks.
- Random-spread placement regions are enumerated once in chunk space without double division.
- FE Matrix registration selects the ResourceLocation overload deterministically.
- Reads live balance settings from config/newworldcore/*.properties.
- Supports deliberate Radar pacing with a configurable placement-batch interval.
- Exposes Radar, Mining, Matrix, Warp, travel, geology, replication, room, and network controls.
- Flushes Geology result buffers before the popup, then flushes the popup as the final foreground GUI layer.
